---
id: td-<topic>
title: <한 줄 테스트 설계 제목>
related: []             # 관련 문서·표준 힌트
covers: []              # 이 설계가 검증하는 요구·사양 항목 (FR-n·NFR-n·SP-n; 여러 문서에서 겹치면 <topic>:FR-1처럼 접두) — ① 참조 무결성 검사 대상
rules: []               # 이 설계가 따르는 rule id — rule 변경 시 재검토 타겟팅
---

# <제목>

> 담는 범위: 이 문서는 **테스트 조건·블랙박스 커버리지 목표·스위트 구조·추적성·절차 원칙**까지만 담는다. 커버리지는 **기법·분할·경계의 식별**까지 — 거기서 뽑은 구체 입력값·기대 출력, 데이터·픽스처·실행 스크립트·환경, 화이트박스 커버리지(결정/분기/MC-DC) 측정은 `test/` 코드의 몫이다(설계/구현 절단선 — `README.md`).

## 범위
<이 테스트 설계가 검증하는 요구·사양의 경계. 무엇을 다루고 무엇을 제외하는가.>

## 테스트 조건
검증되어야 할 조건. 코드가 아니라 "무엇이 참이어야 하는가"를 관찰 가능하게 쓴다. 각 조건이 어느 요구·사양 항목에서 나왔는지는 추적성에서 밝힌다.
- **TD-1** <검증 조건. 어떤 상황에서 무엇이 성립/거부되어야 하는가.>
- **TD-2** <…>

## 커버리지 목표
블랙박스 기법으로 각 조건을 어느 수준까지 덮는지. 해당하는 기법(동등분할·경계값·결정표·상태전이)을 쓰고 목표를 밝힌다. 화이트박스 커버리지는 여기 두지 않는다.
- **TD-1** <적용 기법과 목표. 예: 경계값 — 하한·상한·경계 바로 밖.>

## 스위트 구조
테스트 조건을 어떤 레벨(유닛·통합·시스템)과 그룹으로 조직하는지.
- <스위트/그룹과 그에 속한 조건, 실행 순서·전제(있으면).>

## 추적성
요구·사양 항목 ↔ 테스트 조건 매핑. 대응 조건이 없는 요구·사양(공백), 근거 요구·사양이 없는 조건(고아)이 없는지 드러낸다. 매핑은 frontmatter `covers`에 선언한다(① 참조 무결성 검사 대상); 이 표는 같은 매핑을 공백·고아까지 눈으로 보는 뷰이며 `covers`와 일치해야 한다. 어느 매핑이 타당한지는 검토 ②가 내용으로 판정한다(`covers`·표는 힌트).
- **FR-1 / SP-3** ← **TD-1**, **TD-2**
- <…>

## 절차 원칙
각 조건을 어떻게 검증하는가의 원칙 — 검증 방법·관찰점. 구체 스텝·도구·명령이 아니라 원칙을 쓴다.
- **TD-1** <검증 방법·관찰점.>

## 수용 기준
각 테스트 조건(`TD-n`)의 **합격 판정** — 무엇을 관찰하면 그 조건이 통과인지 관찰 가능하게 확정한다. (이 조건이 설계로서 충분한지의 판단은 검토 ②의 몫이지 여기서 자기주장할 자리가 아니다.)
- [ ] **TD-1** <이 조건의 통과로 볼 관찰 결과.>

## 참조
- 요구사항: `../requirements/<topic>.md`
- 사양: `../specification/<topic>.md`
- <관련 rule·외부 표준(예: ISTQB 테스트 분석·설계).>
